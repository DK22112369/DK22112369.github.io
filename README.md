# Portfolio Source

Static portfolio source for selective direct-link sharing.

The published page is configured with `noindex`, `nofollow`, and `robots.txt`
rules to reduce search-engine discovery. The page also avoids public phone
numbers, detailed address text, external scripts, external fonts, analytics, and
input forms.
